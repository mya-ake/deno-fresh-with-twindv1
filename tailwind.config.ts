import type { Config } from "https://esm.sh/tailwindcss@3.3.2";

const config: Config = {
  theme: {
    extends: {
      colors: {
        transparent: "transparent",
      },
    },
  },
};

export default config;
